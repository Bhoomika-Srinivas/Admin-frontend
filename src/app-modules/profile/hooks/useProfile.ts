// Wraps the existing AuthContext profile update — extend when backend is ready
import { useAuth } from '@/auth/AuthContext'
// Hook implementation TBD
export function useProfile() {
  // TODO: implement with useEffect + state
  return { useAuth }
}
